import { SeeAllListDirective } from './see-all-list-directive';

describe('SeeAllListDirective', () => {
  it('should create an instance', () => {
    const directive = new SeeAllListDirective();
    expect(directive).toBeTruthy();
  });
});
